include ../../../common.mk

DRIVER_NAME := ata_drive
OBJ_DIR     := ../../../output/drivers/$(DRIVER_NAME)

all:
	mkdir -p $(OBJ_DIR)
	$(CC) $(CFLAGS) -c $(DRIVER_NAME).c -o $(OBJ_DIR)/$(DRIVER_NAME).o
